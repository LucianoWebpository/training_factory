{% block nav %}
<header>
    <img src="{{ asset('../img/logo-training-fatory-HD.png') }}" width="100%" height="400"  alt="....">
</header>
<nav class="navbar navbar-expand-lg navbar-light bg-light">

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
{#                {{  link1 }}#}
                <a class="nav-link" href="{{ path('bez_activiteit_show')}}">Home <span class="sr-only">(current)</span></a>
            </li>
            {% if is_granted('ROLE_ADMIN') %}

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('dir_form_show')}}">activiteit toevoegen</a>
                </li>
            {% endif %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('bez_registreren')}}">lid worden</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{ path('contact_show')}}">contact</a>
            </li>
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('lijst_activiteit')}}">activiteiten lijst</a>
                </li>
            {% endif %}

            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_logout')}}">uitloggen</a>
            </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('edit_act')}}">lessen</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('lid_les_show')}}">planning</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('profiel_lid')}}">mijn profiel</a>
                </li>
            {% else %}<li class="nav-item">
                <a class="nav-link" href="{{ path('app_login')}}">inloggen</a>
            </li>
            {% endif %}
            {% if is_granted('ROLE_INSTRUCTEUR') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('inst_form_show')}}">lessen toevoegen</a>
                </li>
            {% endif %}
        </ul>

    </div>
</nav>
{% endblock %}