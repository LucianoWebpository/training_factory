{% extends 'base.html.twig' %}

{% block body %}
    {#<p>activiteit naam: </p>{{ activiteit.naam }} <br>#}
    {#<p>activiteit: beschrijving</p>{{ activiteit.beschrijving }} <br>#}
    {#    <p>activiteit: duur</p> {{ activiteit.duur }} <br>#}
    {#    <p>activiteit: prijs</p>{{ activiteit.prijs }} <br>#}
    <div class="wrapper">
        {% block nav %} {% include 'bezoeker/navbarBezoeker.html.twig' %} {% endblock %}

        <table class="table table-striped table-dark">
            <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">naam</th>
                <th scope="col">beschrijving</th>
                <th scope="col">duur</th>
                <th scope="col">prijs</th>
            </tr>
            </thead>
            <tbody>
            {% for gebr in user %}
            <tr>



                <th scope="row">
                    {{ gebr.id }} </th>
                <td>{{ gebr.email}}</td>
                <td>{{gebr.voornaam}}</td>
                <td>{{gebr.achternaam}}</td>

                <td>{{gebr.geslacht}}</td>
                <td> <a href="{{ path('delete_user', {'id' : gebr.id}) }}"><button> delete</button></a></td>
                <td> <a href="{{ path('aanpassen_user', {'id' : gebr.id}) }}"><button>aanpassen</button></a></td>
                {% endfor %}
            </tr>
            </tbody>
        </table>
    </div>

{% endblock %}
