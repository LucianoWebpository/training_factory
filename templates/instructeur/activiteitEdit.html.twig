{% extends 'base.html.twig' %}

{% block body %}
    {#<p>activiteit naam: </p>{{ activiteit.naam }} <br>#}
    {#<p>activiteit: beschrijving</p>{{ activiteit.beschrijving }} <br>#}
    {#    <p>activiteit: duur</p> {{ activiteit.duur }} <br>#}
    {#    <p>activiteit: prijs</p>{{ activiteit.prijs }} <br>#}
    <div class="wrapper">
        {% block nav %} {% include 'bezoeker/navbarBezoeker.html.twig' %} {% endblock %}

        <table class="table table-striped table-dark">
            <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">naam</th>
                <th scope="col">tijd</th>
                <th scope="col">locatie</th>
                <th scope="col">max personen</th>
                <th scope="col">datum</th>
            </tr>
            </thead>
            <tbody>
            {% for les in lessen %}
            <tr>



                <th scope="row">{{ les.id }} </th>
                <td>{{ les.naam}}</td>
                <td> {{ les.tijd|date('H:i') }}</td>
                <td>{{les.locatie}}</td>
                <td>{{les.maxpersonen}}</td>
                <td>{{ les.tijd|date("d/m/Y") }}</td>
                <td> <a href="{{ path('delete_activiteit', {'id' : les.id}) }}"><button> delete</button></a></td>
                <td> <a href="{{ path('aanpassen', {'id' : les.id}) }}"><button>aanpassen</button></a></td>
                {% endfor %}
            </tr>
            </tbody>
        </table>
    </div>

{% endblock %}
